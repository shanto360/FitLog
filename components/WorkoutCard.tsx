import Link from "next/link";import {Clock,Flame,Star,ArrowUpRight} from "lucide-react";import {Workout} from "@/context/PlanContext";import {imageOf,listOf,nameOf} from "@/lib/workouts";
export default function WorkoutCard({workout:w}:{workout:Workout}){return <Link href={`/workout/${w.id}`} className="workout-card panel group overflow-hidden rounded-md"><div className="card-image relative"><img className="workout-image" src={imageOf(w)} alt={nameOf(w)}/><span className="absolute left-3 top-3 flex flex-wrap gap-1">{listOf(w.category).slice(0,2).map(t=><span className="pill bg-black/70" key={t}>{t}</span>)}</span><span className="absolute bottom-3 right-3 grid h-9 w-9 place-items-center rounded-full bg-acid text-black opacity-0 transition group-hover:opacity-100"><ArrowUpRight size={18}/></span></div><div className="p-4"><h3 className="display min-h-[48px] text-xl font-semibold leading-tight">{nameOf(w)}</h3><p className="mt-2 truncate text-xs text-gray-500">{listOf(w.equipment).join(", ")}</p><div className="mt-5 flex flex-wrap gap-4 border-t border-[#2a2f2b] pt-4"><span className="stat"><Clock size={13}/>{w.duration} min</span><span className="stat"><Flame size={13}/>{w.calories} kcal</span><span className="stat"><Star size={13} className="text-acid"/>{w.rating}</span></div></div></Link>}
